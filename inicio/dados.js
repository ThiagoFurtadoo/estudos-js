//ENG: Data
//Subject: Variables

var nome = "caneta";
var quantidade = 10;
var preco = 6.4;        //Note: Variables can be named as "var" or "let" although there are slight differences.
let imposto = 1.5
let precoFinal = preco + imposto

console.log("caneta");
console.log(quantidade);
console.log(preco); //Note: "quantidade" "preco" and so on, can be interpretated as a "literal value".     
console.log(imposto);
console.log(precoFinal);

var nome = "Caneta Bic"; //Note: Variables can be changed after or midway through the script.
console.log(nome);       // But they both have to be equal, set only as "var" or "let".