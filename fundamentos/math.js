//ENG: Math.

const raio = 5.6
const area = Math.Pi * Math.pow(raio, 2) /*Note: "pow" returns the value which is equal 
                                           to the base raised to the exponent number.*/

console.log(area)
console.log(typeof Math) /*Math namespace object contains static properties
                           and methods for mathematical constants and functions.*/
                         //Math works with the Number type. It doesn't work with BigInt.